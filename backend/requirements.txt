fastapi==0.115.5
uvicorn[standard]==0.32.1
pydantic==2.10.3
python-multipart==0.0.19
anthropic==0.40.0
python-dotenv==1.0.1
qdrant-client[fastembed]==1.12.1
pymupdf==1.25.1
langgraph==0.2.60
langsmith>=0.2.10
# Docling does the policy-PDF parsing (layout-aware markdown). It's a heavy
# install — it pulls torch — so policy_rag.py falls back to PyMuPDF if it isn't
# present, and prints which parser actually ran.
docling>=2.15.1
